import React from 'react';
import { Link } from 'react-router-dom';

const Checkout = () => {
    return (
        <div>
            <h1>Checkout</h1>
            { /* Need to implement checkout form */}
            <Link to="/" class="btn btn-primary">Return</Link>
        </div>
    );
};

export default Checkout;