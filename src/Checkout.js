import React from 'react';

const Checkout = () => {
    return (
        <div>
            <h1>Checkout</h1>
            { /* Need to implement checkout form */}
        </div>
    );
};

export default Checkout;